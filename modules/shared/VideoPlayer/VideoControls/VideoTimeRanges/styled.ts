import styled from 'styled-components';

export const StyledVideoTimeRanges = styled.p`
  font-size: 1.4rem;
  line-height: 1;
  letter-spacing: .1px;
  
  & > span {
    display: inline-block;
    min-width: .6666em;
    text-align: center;
  }
`;